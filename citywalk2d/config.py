"""Central configuration for citywalk2d.

Placeholder only — later nodes add tunables (world size, glyph/palette
choices, timing, input bindings) as they are implemented.
"""

from __future__ import annotations

# Terminal defaults; the renderer node owns real geometry later.
DEFAULT_WIDTH = 80
DEFAULT_HEIGHT = 24
